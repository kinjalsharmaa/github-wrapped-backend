# GitHub Wrapped — Backend

Express API that powers [GitHub Wrapped](https://github.com/kinjalsharmaa/github-wrapped-frontend), fetching and aggregating a GitHub user's public stats.

## Endpoint
`GET /api/github/:username` — returns profile info, language breakdown, and top starred repo.

## Running locally
```bash
npm install
node index.js
```
